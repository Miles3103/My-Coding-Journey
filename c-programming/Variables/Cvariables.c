#include <stdio.h>

int	main(void)
{
	int	i; // Holds signed integers, e.g. -4, -2, 0, 1, 10
	float	f; // Holds sigend floating point numbers, e.g. -3.1416
	char	c; // Holds character, like a, b, ...
	
	i = 2;
	f = 2.55;
	c = 'C';
	printf("%d\n", i);
	printf("%f\n", f);
	printf("%c\n", c);
}
